#!/usr/bin/bash

# Print the 16 colors from the color scheme

# standard colors
for C in {40..47}; do
    printf "\e[${C}m%3s" $C
done

echo

# high intensity colors
for C in {100..107}; do
    printf "\e[${C}m%3s" $C
done

# vi: ft=sh
