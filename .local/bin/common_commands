#!/usr/bin/env bash

# Cat may be replaced by `<(...)`, but is clearer here.
# shellcheck disable=SC2002
cat "$XDG_CACHE_HOME/zsh/history" |
    tr '|;' "\n" |
    sed "s/^ //g" |
    cut -d " " -f 1 |
    sort |
    uniq -c |
    sort -n |
    tail -n 15 |
    tac
